# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.11
matplotlib>=3.7

# Machine learning
scikit-learn>=1.3

# Fuzzy C-Means and TSK systems
scikit-fuzzy>=0.4
scikit-pytsk>=0.0.6

# PyTorch (required by pytsk gradient descent)
torch>=2.0

# Parallelism (used in compression and bagging)
joblib>=1.3

# Data acquisition (downloads UCI datasets on first use)
ucimlrepo>=0.0.3

# Jupyter (for demo notebook)
jupyterlab>=4.0
ipykernel>=6.0
